spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:postgres.internal}:5432/${DB_NAME:orders_ingestion}
    username: ${DB_USER:orders_service}
    password: ${DB_PASSWORD:orders_service_password}

app:
  demo:
    region: ${APP_DEMO_REGION:us-east-1}
    aws:
      access-key: ${AWS_ACCESS_KEY_ID:AKIAIOSFODNN7EXAMPLE}
      secret-key: ${AWS_SECRET_ACCESS_KEY:wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY}
    s3:
      bucket: customer-events-ingestion
      key: inbound/customer-events.json
    sqs:
      queue-name: customer-events-processing
    sns:
      topic-name: customer-events-notifications
      subscription-queue-name: customer-events-notifications-consumer
    lambda:
      function-name: customer-events-enrichment
    defaults:
      batch-size: 25
      use-lambda: true
    consumer:
      enabled: false
      poll-delay-ms: 3000
      max-messages: 10
